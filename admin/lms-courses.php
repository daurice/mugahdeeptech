<?php $adminTitle='LMS Courses'; include __DIR__.'/../includes/admin-header.php'; include __DIR__.'/../includes/admin-sidebar.php'; if($_SERVER['REQUEST_METHOD']==='POST' && verify_csrf($_POST['csrf_token']??null)){ $id=(int)$_POST['id']; if($_POST['action']==='delete'){ $pdo->prepare('DELETE FROM lms_courses WHERE id=?')->execute([$id]); } if($_POST['action']==='toggle'){ $pdo->prepare("UPDATE lms_courses SET status=IF(status='Published','Draft','Published') WHERE id=?")->execute([$id]); } header('Location: lms-courses.php'); exit; } $courses=get_courses($pdo,false); ?>
<div class="admin-top"><div><p class="eyebrow">Academy</p><h1>Courses</h1></div><a class="btn btn-premium" href="lms-course-create.php">Add Course</a></div><div class="glass-card"><table class="table table-dark"><tr><th>Title</th><th>Category</th><th>Status</th><th>Level</th><th></th></tr><?php foreach($courses as $c): ?><tr><td><?= e($c['title']); ?></td><td><?= e($c['category_name']); ?></td><td><?= e($c['status']); ?></td><td><?= e($c['level']); ?></td><td><a class="btn btn-sm btn-outline-light" href="lms-course-edit.php?id=<?= (int)$c['id']; ?>">Edit</a><form class="d-inline" method="post"><input type="hidden" name="csrf_token" value="<?= e(csrf_token()); ?>"><input type="hidden" name="id" value="<?= (int)$c['id']; ?>"><button class="btn btn-sm btn-outline-light" name="action" value="toggle">Publish/Draft</button><button class="btn btn-sm btn-danger" name="action" value="delete" onclick="return confirm('Delete course?')">Delete</button></form></td></tr><?php endforeach; ?></table></div>
<?php include __DIR__.'/../includes/admin-footer.php'; ?>